[project]
name = "image-enhance-genai"
version = "0.1.0"
description = "Crop + upscale + GenAI enhance"
requires-python = ">=3.10"
dependencies = [
  "opencv-python-headless>=4.9.0",
  "numpy>=1.26.0",
]

[tool.uv]
package = false
